apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: hms
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
    spec:
      containers:
        - name: notification-service
          image: hms/notification-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3007
          env:
            - name: PORT
              value: "3007"
            - name: DB_HOST
              value: "db-notifications"
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: hms-config
                  key: DB_PORT
            - name: DB_NAME
              value: "hms_notifications"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-secret
                  key: DB_PASSWORD

          readinessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3007
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              memory: 64Mi
              cpu: 50m
            limits:
              memory: 256Mi
              cpu: 300m
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: hms
spec:
  selector:
    app: notification-service
  ports:
    - port: 3007
      targetPort: 3007
